# ============================================================
# Task ID  : T027
# Title    : Dapr Secret Store component — Kubernetes native
# Spec Ref : speckit.plan → Section 5: State & Secrets Management
# Plan Ref : speckit.plan → Section 5.2: Secrets strategy (kubernetes secretstore)
# ============================================================
{{- if .Values.dapr.enabled }}
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: secretstore-k8s
  namespace: {{ .Values.global.namespace | default "todo-app" }}
  labels:
    {{- include "todo.labels" . | nindent 4 }}
spec:
  type: secretstores.kubernetes
  version: v1
  # No auth metadata needed — uses the pod's Kubernetes service account
  metadata: []
{{- end }}
